{
  "name": "fontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "watch:wasm": "cd src/core/sound/rust-synth && cargo watch -w src -s \"wasm-pack build --target web --out-dir build\"",
    "dev": "concurrently \"npm run watch:wasm\" \"vite\"",
    "build": "tsc && vite build",
    "build:wasm": "cd src/core/sound/rust-synth && wasm-pack build --release --target web --out-dir build",
    "build:server_side": "npm run build:wasm && tsc && vite build",
    "start": "npm run build:server_side"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4",
    "chokidar-cli": "^3.0.0",
    "concurrently": "^9.2.0",
    "sass": "^1.91.0",
    "typescript": "~5.8.3",
    "vite": "^7.1.2"
  },
  "dependencies": {
    "@types/audioworklet": "^0.0.83",
    "fast-diff": "^1.3.0",
    "idb": "^8.0.3",
    "uuid": "^11.1.0"
  }
}
